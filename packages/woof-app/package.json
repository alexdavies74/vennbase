{
  "name": "woof-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@heyputer/puter.js": "^2.2.11",
    "puter-federation-sdk": "workspace:*",
    "yjs": "^13.6.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
